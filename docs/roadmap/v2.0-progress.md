# JeriDevOps v2.0 进度备忘

> 与 [`v2.0-development-plan.md`](./v2.0-development-plan.md) 和 [`v2.0-feature-flags.md`](./v2.0-feature-flags.md) 互补。
> 本文是**执行侧单一真相源**：每完成一项，勾选对应条目并更新"最近动作"小节。
> 最近更新：2026-04-22（E7-06 第十二批清理：移除 `release.risk_scoring` Flag 常量/种子/前端映射与文档条目；`RiskScorer` 改为固定执行（提交审批即打分，不再依赖 Flag 分支）；并保留前十一批 `notify.channel_unification` / `obs.*` / `ai.*` / `app.*` / `platform.workspace_dora` / `planning.jira_as_source` / `legacy.oa_module` / `platform.ia_v2` / `release.legacy_jenkins_deploy` / `release.legacy_k8s_apply` / `release.gitops_enabled` 清理；另补齐安全模块镜像仓库密码 AES-GCM 落库加密与“使用已存凭据测试连接”能力，并对 Jira/Confluence/Nacos/SonarQube/Prometheus 增加历史明文凭据兼容读取、在线自愈转密文回写与 Jira 单实例响应脱敏；补充自愈迁移加密/回写失败 warning 日志可观测性与 `scripts/cleanup_v2_flags.sql` 数据库清理脚本；按优先级推进 Q-01，新增 `registry_service` / `release_service` / `rule_service` / `security_service` / `audit_logger` / `config_checker` 与镜像扫描辅助函数单测，并补齐 `release_service` 的 `List/GetByID` 聚合关联场景（Nacos 子项与 Biz Goal/Version 回填）单测，覆盖率提升到 `internal/service/release=42.5%`、`internal/service/security=33.9%`、`internal/domain/notification/service=75.8%`；同时完成 E4-01 日志统一台全局过滤器（`cluster_id` / `namespace`）在统一页与 Center/Search/Export/Stats/Compare 跨 Tab 联动，并补齐 E7-03 Jira 权威源拉取视图（`BizRequirements` 新增只读提示与 Jira JQL 拉取弹窗，支持默认实例 Issue 列表查询与外链跳转），以及 Webhook 同步增强（`instance_id + project_key` 映射回填 `application_id`、按 `fixVersions` 尝试关联 `version_id`、`external_key` 幂等 upsert/delete）与 DevOps->Jira 状态回写（按 issue key 反查映射、自动选择 transition、回写评论），并新增 `labels/components/epic` 映射落库与需求页筛选展示能力，补充需求列表/详情与目标/版本详情页外部单号 Jira 快捷跳转（统一默认实例解析）及目标/版本详情需求表 Epic 列展示，另为需求列表及目标/版本详情需求子表增加 Jira 扩展列显隐配置（本地记忆 + 一键恢复默认），并抽取 `useBizRequirementColumnPrefs` 复用列偏好逻辑。其余同前次 E6-01 收口/E3/E4-03/E5/E6-02/E7-04/E7-05）

## 1. 完成度速览

| Epic | 计划人日 | 完成度 | 关键状态 |
|---|---|---|---|
| Epic 1 · IA & 菜单 | 16 | ✅ ~95% | V2 菜单 / DORA 工作台 / Feature Flag 中心 / 菜单 Tour 已上线 |
| Epic 2 · GitOps 发布 | 60 | ✅ ~90% | Release 聚合根、ArgoCD、风险评分、DORA 真实数据、事故联动、Postmortem + Confluence 推送完成；Pilot 切换 + legacy 路径下线未做 |
| Epic 3 · 应用中心聚合 | 38 | 🟩 ~92% | `ApplicationDetail` 的「可观测 / 变更事件 / 成本 / 安全」已接真实聚合，剩余主要为统一回归与质量门槛 |
| Epic 4 · 观测瘦身 | 21 | 🟧 ~65% | 事件时间线 + 告警中心 V2 + 日志统一台（`/logs/unified` 七 Tab + `obs.unified_logs` 旧路径重定向）；Alert / Approval 已并入时间线 |
| Epic 5 · AI Copilot | 20 | 🟩 ~85% | 全局 Copilot + 上下文；E5-03 工具条预读 Release / Incident / DORA 并注入会话 |
| Epic 6 · 治理压缩 | 14 | 🟩 ~85% | `/message/channel` 统一入口 + 目录写接口/管理页；统一通道凭据加密/验证/测试发送已落地；安全概览增加供应链 / Sonar 聚合跳转 |
| Epic 7 · 遗留清理 & GA | 19 | 🟧 ~45% | E7-01~03 已落地；E7-04 提供 `scripts/migrate_legacy_release_dryrun.sql`；E7-05 旧通知/OA 类路径见 `router/index.ts`（SPA redirect）；E7-06 与 GA 全量删 Flag 留待 S7+30d |
| QA & 压测 & 回归 | 25 | 🟥 0% | 未启动 |
| 文档 / 培训 / Buffer | 32 | 🟧 部分 | ADR / Flag 清单齐备，培训材料未写 |

**剩余 ≈130–150 人日（约 3~4 Sprint）。**

## 2. 已完成（可回溯的关键动作）

- **S1 · IA 重构**
  - V2 7-域菜单 (`web/src/config/menu.v2.ts`)
  - V2 菜单管理后台（已在 E7-06 收口为固定 V2）
  - 工作台 DORA 卡片 + 双周对比 Sparkline
  - V1↔V2 切换自动 Tour
- **S2-S3 · GitOps 发布**
  - `internal/models/deploy/release.go` 聚合根（吞并 Deploy/Promotion）
  - ArgoCD 模块：实例 / 应用 / 同步（`internal/service/argocd/`）
  - Release → PR 自动生成器（`internal/service/pipeline/gitops_handoff_service.go`）
  - 变更风险评分 + 可视化
- **DORA 真实数据闭环**
  - `internal/service/metrics/dora_aggregator.go`：四指标 + 应用 vs 全站对比（`app_vs_fleet`）
  - Dashboard 卡片 + 应用维度钻取
- **事故 & 复盘**
  - `Incident` 模型 + 列表/详情页
  - Alert → Incident 联动（`IncidentService.UpsertFromAlert` + `IncidentRepository.GetByFingerprint`）
  - Release 详情 → 关联事故 Tab + 内嵌登记 Modal
  - Postmortem Markdown 导出
  - Postmortem 一键推送 Confluence（`internal/service/confluence/` + `internal/service/incident/confluence_publisher.go`）
- **OA 模块物理删除**
- **S4 · 应用中心聚合（进行中）**
  - `ApplicationDetail.vue`：「可观测 / 变更事件」已接统一时间线聚合（按应用过滤 Incident / Alert / Approval / ChangeEvent / Release）；「成本 / 安全」已接真实聚合（应用成本画像 / 环境基线 / 优化建议、镜像扫描摘要 / 漏洞详情 / 归属来源标记）
- **S5a · 告警中心 V2（Epic 4 / E4-02）**
  - `web/src/views/alert/AlertCenterV2.vue` 聚合 AlertConfig / MessageTemplate / AlertSilence / AlertEscalation
  - `obs.alert_center_v2`：`/alert/config` 等 → `/alert/center?tab=`；V2 侧栏「告警中心」→ `/alert/center`
- **S5 · 事件时间线（Epic 4 / E4-03）**
  - `pkg/dto/timeline.go` + `internal/service/observability/timeline_service.go`
  - `internal/modules/monitoring/handler/timeline_handler.go`：`GET /observability/timeline`
  - `web/src/views/observability/EventTimeline.vue` + V2 菜单「事件时间线」
  - 应用详情「可观测」Tab 增加跳转事件时间线（带 `application_id`）
- **S5b · 日志统一台（Epic 4 / E4-01）**
  - `web/src/views/logs/LogsUnifiedConsole.vue`：`/logs/unified?tab=`（center / search / export / stats / compare / bookmarks / alerts）
  - `obs.unified_logs`：`router` 守卫重定向旧子路径；V2 侧栏「日志」→ `/logs/unified`；`MainLayout` 侧栏高亮聚合
- **S6a · Copilot 全局化（Epic 5 / E5-01）**
  - `web/src/components/ai/CopilotDock.vue`（由原悬浮窗升级）：`MainLayout` 全业务页挂载；已收口为固定 Copilot 品牌与「上下文感知」状态文案（无 Flag 分支）
  - 旧书签 `/ai/chat` → `/dashboard` 重定向；`AIChatWidget` 导出名保留为 `CopilotDock` 别名
- **S6b · Copilot 上下文（Epic 5 / E5-02）**
  - `web/src/composables/useCopilotContext.ts`：`buildCopilotSnapshot` / `mergePageContextWithCopilot` / `useCopilotContext().snapshot`
  - `CopilotDock` 固定将快照写入 `PageContext.extra_data.copilot`（含 `applicationId` / `env` / `entityId` / `entityKind` / `timeRange`）

## 3. 剩余功能清单（可直接拆为 Todo 执行）

### 3.1 Epic 3 · 应用中心聚合 — 🟥 最大头，M3 关键路径

- [x] **E3-04** `ApplicationDetail.vue` 聚合应用关键视图，并在 E7-06 收口为固定路径（无 Flag 分支）

### 3.2 Epic 4 · 观测瘦身 — 🟧 进行中

- [x] **E4-01** 日志统一查询台 `obs.unified_logs`：`LogsUnifiedConsole.vue`（`/logs/unified` 七 Tab 嵌入既有子页）+ 旧 `/logs/*`（除 `viewer`→`center` 链）`beforeEnter` 重定向 + V2 菜单入口改写；已补齐全局过滤器（`cluster_id` / `namespace`）跨 Tab 联动
- [x] **E4-02** 告警中心 V2 `obs.alert_center_v2`：`/alert/center` 四 Tab + Flag 开启时旧路径重定向 + V2 菜单入口改写；概览仍保留 `/alert/overview`
- [x] **E4-03** 事件时间线 `obs.incident_timeline`：`GET /observability/timeline` + `/observability/event-timeline` 页已聚合 Incident / Alert / Approval / ChangeEvent / Release

### 3.3 Epic 5 · AI Copilot（ADR-0006） — 🟧 进行中

- [x] **E5-01** 全局悬浮：`CopilotDock.vue` 替代 `AIChatWidget`（`MainLayout` 常驻）；Copilot 品牌与状态文案固定启用；`/ai/chat` 重定向至工作台；独立对话菜单本仓库历史上未注册路由，仅兼容旧 URL
- [x] **E5-02** 上下文自动采集：`useCopilotContext()` + `buildCopilotSnapshot` / `mergePageContextWithCopilot`；`CopilotDock` 固定把 `{ applicationId, env, entityId, entityKind, timeRange }` 写入 `extra_data.copilot` 并回填 `app_id`
- [x] **E5-03** `CopilotDock` 展示「工具」条：一键拉取 Release 列表 / Incident 列表 / DORA 快照并作为用户消息注入当前会话（调用既有 REST）

### 3.4 Epic 6 · 治理压缩

- [x] **E6-01（MVP）** 前端 `/message/channel` + 菜单；**后端** `GET /message/channels` 合并 `notification_channels` 目录表与 `telegram_bots`（及钉钉/企微/飞书占位）；模型 `NotificationChannel` + `AutoMigrate`
- [x] **E6-01（增强）** 统一目录写接口已落地：`POST/PUT/DELETE /message/channels` + `ChannelUnified.vue` 单页管理；Webhook 型 provider 已支持加密凭据、摘要展示、`POST /message/channels/:id/test` 测试发送与 `POST /message/channels/validate` 保存前验证，并补齐服务层/Handler 层单测（凭据归一化、AES-GCM、`clear_credential`、provider 响应判定）；Telegram 继续走专属页
- [x] **E6-02（MVP）** `SecurityOverview.vue` 增加「供应链与代码质量」聚合卡片：跳转镜像仓库 / 镜像扫描 / SonarQube，SBOM 占位

### 3.5 Epic 7 · 遗留清理 & v2.0 GA 硬化

- [x] **E7-01** Legacy Jenkins 直发下线：`deploy_service` 在 `CreateDeploy` / `CreateDeployWithEmergency` / `CreateDeployRecord` 与 `executeJenkinsDeploy` 中统一拒绝 Jenkins 直发（固定返回 GitOps 提示），不再依赖 `release.legacy_jenkins_deploy` 分支
- [x] **E7-02** Legacy K8s 直发下线：`deploy_service` 与 `ExecutorEngine` 对 K8s 直发/`k8s_deploy` 步骤统一固定拒绝并提示 GitOps，不再依赖 `release.legacy_k8s_apply` 分支
- [~] **E7-03（增强进行中）** 规划权威源切换：`GET /biz/planning/source` 固定返回 `jira`；Biz 目标/需求/版本 **POST/PUT/DELETE** 固定返回 403（只读门面）；已补齐需求页 Jira 拉取视图（JQL 查询 + 默认实例 + Issue 外链）与 `POST /biz/planning/jira/webhook` 同步入口（按 `external_key` 幂等 upsert/delete，支持 `instance_id + project_key` 映射回填应用、`fixVersions` 关联版本，并映射 `labels/components/epic` 字段），并补齐 DevOps->Jira 状态回写（自动 transition + comment，失败仅告警不阻断本地）；后续补充治理策略与更多字段规范
- [x] **E7-04（DRY-RUN）** 仓库内提供 `scripts/migrate_legacy_release_dryrun.sql`（注释模板 + 清单说明）；真实迁移需在业务库验证后执行
- [x] **E7-05（SPA）** 旧路径客户端重定向已在 `web/src/router/index.ts`：`/ai/chat`→工作台、`/feishu|dingtalk|wechatwork/message`→Telegram 或统一页、`/message/channel` 为统一入口
- [x] **E7-06** Feature Flag 生命周期收尾：S7+30d 移除所有 v2 Flag 代码分支（已完成第一批 `notify.channel_unification`、第二批 `obs.unified_logs` / `obs.alert_center_v2` / `obs.incident_timeline`、第三批 `ai.global_copilot` / `ai.auto_context`、第四批 `app.unified_detail`、第五批 `platform.workspace_dora`、第六批 `planning.jira_as_source`、第七批 `legacy.oa_module`、第八批 `platform.ia_v2`、第九批 `release.legacy_jenkins_deploy`、第十批 `release.legacy_k8s_apply`、第十一批 `release.gitops_enabled`、第十二批 `release.risk_scoring` 清理）

### 3.6 质量门槛 & 交付物

- [ ] **Q-01** 后端单测覆盖率 ≥ 60%（关键服务 ≥ 80%）
- [ ] **Q-02** E2E 场景 ≥ 30 个全绿（发布闭环、告警联动、回滚、审批、发布切换等）
- [ ] **Q-03** 性能回归：应用详情首屏 ≤ 1.5s，发布单 PR ≤ 5s，核心 API P99 ≤ 500ms
- [ ] **Q-04** 敏感字段加密审计（Jira / Confluence / Nacos / Sonar / Prometheus 实例表 100% AES-GCM）
- [ ] **Q-05** 3 个 Pilot 应用切换到 GitOps 并采集 DORA 对比
- [ ] **Q-06** 文档 & 培训：《变更说明》《Argo CD 迁移指南》《AI Copilot 使用手册》+ 2 场培训

## 4. 推荐推进顺序

1. Epic 3 骨架（应用中心聚合页）—— 用户感知最强，M3 关键路径
2. Epic 4 观测瘦身（从告警中心 V2 起步，`incident_timeline` 后端已齐备）
3. Epic 5 Copilot 全局化 + 上下文采集（工作量小、体验收益高）
4. Epic 6 通知通道统一
5. Epic 7 遗留清理 + 数据迁移 + Pilot
6. 质量门槛 + 文档 + GA 硬化

## 5. 维护约定

- 每完成 1 个条目，PR 中一并勾选对应 `[ ]` → `[x]` 并更新"最近更新"日期
- 新增 Feature Flag 必须**同时**更新 `v2.0-feature-flags.md` + `internal/service/feature/v2_flags.go` + 本文
- Epic 完成度数字仅估算用，不需精确到百分位
