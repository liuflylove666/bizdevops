replicaCount: 1

image:
  repository: localhost:5001/jeridevops/ui-runner-smoke-0505-go
  tag: bootstrap
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
